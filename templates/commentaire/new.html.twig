{% extends 'base.html.twig' %}

{% block title %}Nouveau commentaire{% endblock %}

{% block body %}
<div class="surface-card p-4">
    <h1 class="mb-4">Créer un commentaire</h1>
    <div class="card shadow-sm">
        <div class="card-body p-4">
            {{ include('commentaire/_form.html.twig', { cancel_path: path('app_commentaire_index'), submit_label: 'Créer' }) }}
        </div>
    </div>
</div>
{% endblock %}
