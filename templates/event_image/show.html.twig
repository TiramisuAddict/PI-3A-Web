{% extends 'base.html.twig' %}

{% block title %}Image #{{ event_image.idImage }}{% endblock %}

{% block body %}
<div class="surface-card p-4">
    <h1 class="mb-4">Image événement #{{ event_image.idImage }}</h1>
    <div class="card border shadow-sm">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-striped table-bordered mb-0">
                    <tbody>
                        <tr><th class="w-25">ID</th><td>{{ event_image.idImage }}</td></tr>
                        <tr><th>Chemin image</th><td class="text-break"><code>{{ event_image.imagePath }}</code></td></tr>
                        <tr><th>Ordre</th><td>{{ event_image.ordre }}</td></tr>
                        <tr><th>Post</th><td>{% if event_image.post %}#{{ event_image.post.idPost }}{% else %}—{% endif %}</td></tr>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
    <div class="d-flex flex-wrap gap-2 mt-4">
        <a class="btn btn-primary" href="{{ path('app_event_image_index') }}"><i class="ti ti-list me-1"></i>Retour</a>
        <a class="btn btn-warning" href="{{ path('app_event_image_edit', { id_image: event_image.idImage }) }}"><i class="ti ti-edit me-1"></i>Modifier</a>
        {{ include('event_image/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
